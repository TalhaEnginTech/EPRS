<h2 class="heading">ReviewForm#index</h2>
<%if user_signed_in? %>

<table class="table table-striped table-hover">
  <thead class="thead-dark ">
  <tr>

    <th>Remarks</th>
    <th>Submitted By</th>

  </tr>
  </thead>
  <tbody>
  <% @rf.each do |q| %>
    <tr>

   <%if current_user.role=="HR" %>


      <td><%= q.remarks %></td>
      <td><%= q.submittedBy %></td>
        <%else %>

      <%end %>
    </tr>
  <% end %>
  </tbody>
</table>
  <%end %>
<%= link_to 'Back',  new_review_form_path, class:"nav-link" %>